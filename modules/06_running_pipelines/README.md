# Module 6 — Running LLM pipelines in research settings

The infrastructure to run analyses repeatably: project base, configs,
run folders, logs, evals, and gold examples.

**Take-home:** a project skeleton + run manifest + eval fixtures.

**Status:** to be built. `example.py` will import from `lab_llm`.
