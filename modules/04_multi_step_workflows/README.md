# Module 4: Multi-step LLM workflows

Split complex tasks into inspectable stages (extract → synthesize →
summarize) with intermediate artifacts you can open and check.

**Take-home:** a workflow template with intermediate artifacts.

**Status:** to be built. `example.py` will import from `lab_llm`.
