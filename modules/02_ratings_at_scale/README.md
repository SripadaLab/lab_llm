# Module 2: LLM ratings at scale

Turn one working prompt into a repeatable rating pipeline over
transcripts × items, with an analysis-ready output table.

**Take-home:** a batch runner + a stable output-table contract
(row IDs, prompt version, model, result columns).

**Status:** to be built. `example.py` will import from `lab_llm`
and read from the shared `../../data/`.
