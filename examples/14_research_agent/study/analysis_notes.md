# Analysis notes

The saved ratings were generated with model `gpt-5.4-mini` and prompt version
`1.3`. The team planned to reconcile documentation after the pilot.

No exclusions have been applied to `ratings.csv`.
